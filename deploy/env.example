# ============================================
# KindMinds Production Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# cp env.example .env
# nano .env

# ============================================
# Frontend Environment Variables
# ============================================
# These are public variables (exposed to browser)
# Get these from your Supabase project settings

NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Backend URL - should be your domain with /api prefix
# Example: https://kindminds.com/api or https://api.kindminds.com
NEXT_PUBLIC_BACKEND_URL=https://yourdomain.com/api

# Fallback backend URL (for development/testing)
NEXT_PUBLIC_BACKEND_FALLBACK=http://localhost:8000

# ============================================
# Backend Environment Variables
# ============================================

# Groq API Key (Required)
# Get from: https://console.groq.com/
GROQ_API_KEY=your_groq_api_key_here

# Hugging Face API Key (Optional - for sentiment analysis fallback)
# Get from: https://huggingface.co/settings/tokens
HUGGINGFACE_API_KEY=your_huggingface_api_key_here

# Supabase Service Role Key (Optional - for admin operations)
# WARNING: Keep this secret! Never expose to frontend
# Get from: Supabase Dashboard > Settings > API
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_here

# CORS Origins - comma-separated list of allowed origins
# Include your production domain and any staging domains
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,http://localhost:3000

# ============================================
# Notes
# ============================================
# - All NEXT_PUBLIC_* variables are exposed to the browser
# - Never commit .env file to version control
# - Restart containers after changing environment variables:
#   docker-compose down && docker-compose up -d

